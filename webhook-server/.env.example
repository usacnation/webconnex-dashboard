# Example env for webhook server

# Secret used to verify webhook payloads (set this to the same secret you configure in Webconnex)
WEBHOOK_SECRET=your_webhook_secret_here

# Where the dashboard should contact the webhook server when fetching recent registrations
VITE_WEBHOOK_SERVER=http://localhost:5000

# Optional: Port to listen on (defaults to 5000)
PORT=5000
