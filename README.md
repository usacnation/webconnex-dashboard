# Create a new directory
mkdir webconnex-dashboard
cd webconnex-dashboard

# Initialize git
git init

# Add remote
git remote add origin https://github.com/usacnation/webconnex-dashboard.git

# Create initial commit
git commit --allow-empty -m "Initial commit"

# Push to GitHub
git push -u origin main
